apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "heml-charts.fullname" . }}-test-connection"
  labels:
    {{- include "heml-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "heml-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
