apiVersion: v1
kind: Service
metadata:
  name: {{ include "heml-charts.fullname" . }}
  labels:
    {{- include "heml-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "heml-charts.selectorLabels" . | nindent 4 }}
